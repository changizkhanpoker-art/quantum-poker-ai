name: Build Quantum AI APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # این قابلیت اجرای دستی رو میده

jobs:
  build-android:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: 📦 Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          python3-pip \
          openjdk-11-jdk \
          zip \
          unzip \
          git \
          libncurses5 \
          libtinfo5
        
    - name: 🔧 Install Buildozer
      run: |
        python -m pip install --upgrade pip
        pip install buildozer cython kivy
        
    - name: 🏗️ Build Android APK
      run: |
        echo "🚀 شروع ساخت ابرهوش کوانتومی..."
        buildozer init
        buildozer android debug
        echo "✅ ساخت APK کامل شد!"
        
    - name: 📱 Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: Quantum-Poker-AI-APK
        path: bin/*.apk
        retention-days: 30
        
    - name: 🎉 Show build success
      run: |
        echo "🧠 ابرهوش کوانتومی ساخته شد!"
        echo "📱 فایل APK در بخش Artifacts قابل دانلود است"
        echo "🔧 برای نصب: Settings → Security → Unknown Sources"
